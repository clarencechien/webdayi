# Database Archive

此目錄存放實驗版本與替代版本的資料庫，保留供未來研究與參考使用。

## 目錄結構

```
data/archive/
├── ngram_blended_experiments/  # Session 9 混合模型實驗
│   ├── ngram_blended.json                    (5.5MB) - v1.2-strict 部署版
│   ├── ngram_blended_v1.1.json               (1.7MB) - v1.1 基準版
│   ├── ngram_blended_v1.1_smoothed.json      (1.7MB) - v1.1 + Action 1 (Laplace)
│   ├── ngram_blended_v1.2.json               (2.7MB) - v1.2 開發版
│   ├── ngram_blended_v1.2_strict.json        (1.7MB) - v1.2-strict (Action 1+2)
│   └── ngram_blended_v1.3_formal.json        (5.4MB) - v1.3-formal (Action 1+2+3)
└── ngram_alternatives/         # 其他替代版本
    └── ngram_db_taiwan.json                  (5.4MB) - 台灣語料特化版
```

## 版本說明

### N-gram Blended Model Experiments (Session 9)

**背景**：Session 9 進行混合模型優化實驗，目標是在檔案大小與準確度之間取得平衡。

**版本演進**：

1. **v1.0** (未歸檔，已刪除)
   - 檔案大小：0.73MB
   - Bigrams：42,956
   - 預期品質：50.0%
   - 狀態：❌ 基準版本，品質不足

2. **v1.1** (`ngram_blended_v1.1.json`)
   - 檔案大小：1.7MB
   - Bigrams：116,672
   - 預期品質：59.3%
   - 變更：參數優化 (ptt_weight=0.3, essay_weight=0.7, topk=40)
   - 狀態：⚠️ 基準版本，但缺少 Laplace 平滑參數

3. **v1.1-smoothed** (`ngram_blended_v1.1_smoothed.json`)
   - Action 1: 加入 Laplace 平滑參數
   - 預期品質：~69% (+10%)
   - 變更：新增 smoothing_alpha, total_chars, vocab_size
   - 狀態：✅ 參考版本

4. **v1.2-strict** (`ngram_blended_v1.2_strict.json`)
   - Action 1+2: Laplace 平滑 + 嚴格清洗
   - 檔案大小：1.7MB
   - Bigrams：116,812
   - 預期品質：**~75%** (+16% over v1.0)
   - 變更：移除 13.81% PTT 噪音（空格、注音、全形標點）
   - 狀態：✅ **推薦版本（平衡）**

5. **v1.2-strict (deployed)** (`ngram_blended.json`)
   - 檔案大小：5.5MB（包含完整 metadata）
   - 與 v1.2_strict.json 相同內容，但格式化更完整
   - 狀態：✅ **曾部署為生產版本**（後被 ngram_db.json 取代）

6. **v1.3-formal** (`ngram_blended_v1.3_formal.json`)
   - Action 1+2+3: Laplace + 嚴格清洗 + 權重調整 (80:20)
   - 檔案大小：5.4MB
   - Bigrams：114,347
   - 預期品質：~78% (正式) / ~72% (聊天)
   - 狀態：✅ 可選版本（商業/學術用途）

**實驗結論**：
- v1.2-strict 為最佳平衡版本（1.7MB, ~75%）
- v1.3-formal 適合正式場合，但聊天品質略降
- **最終決策**：v11.3.5 採用完整 `ngram_db.json` (16MB, 94.4%)，不使用混合模型

**相關文件**：
- `docs/design/NGRAM-BLENDED-EXPERIMENTS.md` - 完整實驗報告
- `docs/design/DESIGN-ngram-blended.md` - 混合模型設計

### N-gram Alternatives

1. **ngram_db_taiwan.json** (5.4MB)
   - 說明：台灣語料特化版本實驗
   - 狀態：實驗版本，未用於生產

## 生產版本 (mvp1/)

目前生產使用的資料庫位於 `mvp1/` 目錄：

1. **dayi_db.json** (743KB)
   - 大易字典資料庫
   - 來源：Rime dayi.dict.yaml
   - 轉換器：converter/convert-v2.js (含頻率排序)

2. **ngram_db.json** (16MB) ← **目前生產使用**
   - 完整 N-gram 語言模型
   - 來源：rime-essay/essay.txt (717M chars)
   - 統計：279K bigrams, 18K unigrams
   - 品質：v2.7 Hybrid 演算法達 **94.4% 準確度**
   - 建構器：converter/build_ngram.py

3. **ngram_pruned.json** (3.2MB)
   - 壓縮版 N-gram（80/20 法則）
   - 統計：42K bigrams (保留 15%, 提供 87% 準確度)
   - 用途：**預備用於 MVP 2a (Chrome Extension)**
   - 品質分數：86.8%

## 版本歷史時間軸

```
2025-11-10  Session 8: N-gram 剪枝優化
            └─ 建立 ngram_pruned.json (16MB → 3.2MB)

2025-11-11  Session 9: 混合模型優化
            ├─ v1.0 → v1.1 → v1.1-smoothed
            ├─ v1.2-strict (Action 1+2)
            └─ v1.3-formal (Action 1+2+3)

2025-11-12  Session 10: v2.7 Hybrid 演算法
            ├─ 發現混合模型過度剪枝導致選字錯誤
            ├─ 切換至完整 ngram_db.json
            └─ 達成 94.4% 準確度（生產就緒）
```

## 未來用途

### MVP 2a (Chrome Extension)

Chrome Extension 需要平衡檔案大小與準確度：

**候選方案**：
1. **ngram_pruned.json** (3.2MB, 86.8%) - 推薦
2. **ngram_blended v1.2-strict** (1.7MB, ~75%) - 備選

**決策因素**：
- Chrome Extension 建議 < 5MB
- ngram_pruned.json 採用智慧剪枝（保留高頻 bigrams）
- 可提供可接受的準確度與檔案大小平衡

### 學術研究

歸檔的混合模型版本可用於：
- 語料庫混合策略研究
- 剪枝演算法比較
- 準確度 vs 檔案大小權衡分析

## 維護說明

- ✅ **保留**：所有歸檔檔案供未來參考
- ⚠️ **不部署**：這些檔案不應用於生產環境
- 📝 **文件化**：每個版本都有對應的設計文件

---

**最後更新**：2025-11-12 (Session 10 資料庫整理)
**維護者**：WebDaYi 開發團隊
